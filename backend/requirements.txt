# ---- Core web framework ----
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# ---- Database ----
SQLAlchemy==2.0.35
alembic==1.13.2

# ---- NLP / hybrid matching (lightweight, always installed) ----
rapidfuzz==3.9.7          # fast fuzzy string matching (falls back to stdlib difflib if not installed)

# NOTE: the optional semantic-AI layer (sentence-transformers/MiniLM) is
# NOT in this file on purpose -- it pulls in PyTorch (a very large,
# slow, occasionally version-fussy download) and is not required for the
# app to run correctly. See requirements-optional.txt to add it later.
# The app detects its absence automatically and falls back to normalized +
# fuzzy matching -- see backend/app/translation/semantic.py.

# ---- Utilities ----
python-dotenv==1.0.1

# ---- Testing ----
pytest==8.3.3
httpx==0.27.2
pytest-cov==5.0.0
