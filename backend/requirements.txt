# MODIFIED FILE -- your existing backend/requirements.txt with the "Auth"
# section added at the bottom. Everything above is identical to your
# current file (with the sentence-transformers package staying in the
# separate requirements-optional.txt, unchanged).

# ---- Core web framework ----
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# ---- Database ----
SQLAlchemy==2.0.35
alembic==1.13.2

# ---- NLP / hybrid matching (lightweight, always installed) ----
rapidfuzz==3.9.7          # fast fuzzy string matching (falls back to stdlib difflib if not installed)

# ---- Utilities ----
python-dotenv==1.0.1

# ---- Testing ----
pytest==8.3.3
httpx==0.27.2
pytest-cov==5.0.0

# ---- NEW: Auth (login/register) ----
bcrypt==4.2.0              # password hashing
PyJWT==2.9.0                # access tokens
email-validator==2.2.0       # enables the EmailStr type used by auth_schemas.py
